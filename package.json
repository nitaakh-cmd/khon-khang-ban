{
  "name": "khon-khang-ban",
  "version": "0.1.0",
  "private": true,
  "description": "Realtime party game MVP for คนข้างบ้าน.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
